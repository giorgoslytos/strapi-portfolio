{
  "name": "bootstrap-jquery-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass sass/main.scss style.css",
    "build:watch": "npm run build && sass  --watch sass/main.scss style.css",
    "build:dev": "sass  --style compressed sass/main.scss style.css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.4.1",
    "jquery": "^3.4.1",
    "pulltorefreshjs": "^0.1.20"
  },
  "devDependencies": {
    "sass": "^1.25.0"
  }
}
